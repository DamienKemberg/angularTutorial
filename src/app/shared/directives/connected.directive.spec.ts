import { ConnectedDirective } from './connected.directive';

describe('ConnectedDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectedDirective();
    expect(directive).toBeTruthy();
  });
});
